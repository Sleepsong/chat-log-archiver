# This file lists the Python packages required by the application.
# When building the Docker image (or setting up a virtual environment),
# these packages will be installed using a command like `pip install -r requirements.txt`.
Flask==2.0.1
Werkzeug==2.0.1
requests==2.26.0